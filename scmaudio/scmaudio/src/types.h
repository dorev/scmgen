#pragma once

#include <string>
#include <vector>
#include <stdint.h>

#include "defines.h"

namespace ScmAudio
{
    using uint32 = uint32_t;
    using String = std::string;
    template<class T>
    using Vector = std::vector<T>;
}